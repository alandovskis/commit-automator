#!/usr/bin/env bash
set -euo pipefail

ACTION=${1}
shift

case $ACTION in
     install)
        DIR=${1}
        shift
        HOOKS="${DIR}/.git/hooks"
        mkdir -p "${HOOKS}"
        cp "hooks/prepare-commit-msg" "${HOOKS}/."
        ;;
esac
